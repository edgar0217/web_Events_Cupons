<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet" />
    <style>
      input[type="file"]::file-selector-button {
        @apply bg-blue-500 text-white py-2 px-4 rounded cursor-pointer hover:bg-blue-600 transition;
      }
    </style>
  </head>

  <body class="bg-gray-100 text-gray-900 min-h-screen flex flex-col">
    <div class="max-w-7xl mx-auto p-6 w-full flex-1">
      <header
        class="flex flex-col md:flex-row justify-between items-center mb-12"
      >
        <h1 class="text-4xl font-extrabold text-slate-800">
          🎉 Panel de Administración
        </h1>
        <div class="flex items-center space-x-6 mt-4 md:mt-0">
          <span class="text-lg"
            >Hola, <span class="font-bold"> <%= user.username %> </span></span
          >
          <a href="/logout" class="text-red-500 hover:underline font-semibold"
            >Cerrar sesión</a
          >
        </div>
      </header>

      <section
        data-aos="fade-up"
        class="bg-white p-8 rounded-3xl shadow-lg mb-16"
      >
        <h2 class="text-3xl font-bold mb-8 text-blue-700">
          ➕ Crear Nuevo Evento
        </h2>
        <form
          action="/eventos"
          method="POST"
          enctype="multipart/form-data"
          class="space-y-6"
        >
          <input
            type="text"
            name="titulo"
            placeholder="Título del evento"
            required
            class="w-full p-4 border rounded-lg text-gray-900 focus:outline-none focus:ring-4 focus:ring-blue-300"
          />
          <textarea
            name="descripcion"
            placeholder="Descripción"
            required
            class="w-full p-4 border rounded-lg text-gray-900 focus:outline-none focus:ring-4 focus:ring-blue-300"
          ></textarea>
          <input
            type="file"
            name="imagen"
            accept="image/*"
            required
            class="w-full"
          />
          <button
            class="w-full bg-blue-600 text-white py-3 rounded-lg hover:bg-blue-700 transition font-bold text-lg"
          >
            Publicar Evento
          </button>
        </form>
      </section>

      <section class="mb-20">
        <h2
          class="text-3xl font-bold mb-8 text-slate-700"
          data-aos="fade-right"
        >
          📅 Eventos Publicados
        </h2>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
          <% listaEventos.forEach(evento=> { %>
          <div
            class="bg-slate-50 p-6 rounded-2xl shadow-md hover:shadow-xl transition transform hover:scale-105"
            data-aos="fade-up"
          >
            <img
              src="<%= evento.imagen_url %>"
              alt="Evento"
              class="w-full h-48 object-cover rounded-xl mb-4"
            />
            <h3 class="text-2xl font-semibold mb-2"><%= evento.titulo %></h3>
            <p class="text-gray-600 mb-4"><%= evento.descripcion %></p>

            <form
              action="/eventos/<%= evento.id %>/edit"
              method="POST"
              enctype="multipart/form-data"
              class="space-y-3"
            >
              <input
                type="text"
                name="titulo"
                value="<%= evento.titulo %>"
                class="w-full p-2 border rounded-lg text-gray-900 focus:ring-4 focus:ring-yellow-300"
                required
              />
              <textarea
                name="descripcion"
                class="w-full p-2 border rounded-lg text-gray-900 focus:ring-4 focus:ring-yellow-300"
                required
              >
<%= evento.descripcion %></textarea
              >
              <input
                type="file"
                name="imagen"
                accept="image/*"
                class="w-full"
              />
              <button
                class="w-full bg-yellow-500 text-white py-2 rounded-lg hover:bg-yellow-600 transition font-semibold"
              >
                Guardar Cambios
              </button>
            </form>

            <form
              id="form-eliminar-<%= evento.id %>"
              action="/eventos/<%= evento.id %>/delete"
              method="POST"
              class="mt-2"
            >
              <button
                type="button"
                onclick="confirmarEliminacion('<%= evento.id %>')"
                class="w-full bg-red-500 text-white py-2 rounded-lg hover:bg-red-600 transition font-semibold"
              >
                Eliminar Evento
              </button>
            </form>
          </div>
          <% }) %>
        </div>
      </section>

      <section
        data-aos="fade-up"
        class="bg-emerald-50 p-8 rounded-3xl shadow-lg mb-16"
      >
        <h2 class="text-3xl font-bold mb-8 text-emerald-700">
          🏷️ Crear Nuevo Cupón
        </h2>
        <form action="/cupones" method="POST" class="space-y-6">
          <input
            type="text"
            name="titulo"
            placeholder="Título del cupón"
            required
            class="w-full p-4 border rounded-lg text-gray-900 focus:outline-none focus:ring-4 focus:ring-emerald-300"
          />
          <textarea
            name="descripcion"
            placeholder="Descripción"
            required
            class="w-full p-4 border rounded-lg text-gray-900 focus:outline-none focus:ring-4 focus:ring-emerald-300"
          ></textarea>
          <input
            type="text"
            name="codigo_cupon"
            placeholder="Código"
            required
            class="w-full p-4 border rounded-lg text-gray-900 focus:outline-none focus:ring-4 focus:ring-emerald-300"
          />
          <button
            class="w-full bg-emerald-600 text-white py-3 rounded-lg hover:bg-emerald-700 transition font-bold text-lg"
          >
            Crear Cupón
          </button>
        </form>
      </section>

      <section>
        <h2 class="text-3xl font-bold mb-8 text-slate-700" data-aos="fade-left">
          🎁 Cupones Disponibles
        </h2>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
          <% listaCupones.forEach(cupon=> { %>
          <div
            class="bg-white p-6 rounded-2xl shadow-md hover:shadow-xl transition transform hover:scale-105"
            data-aos="fade-up"
          >
            <h3 class="text-2xl font-semibold text-emerald-700 mb-2">
              <%= cupon.titulo %>
            </h3>
            <p class="text-gray-600 mb-2"><%= cupon.descripcion %></p>
            <p
              class="font-mono bg-emerald-100 text-emerald-800 px-3 py-1 rounded inline-block mb-4"
            >
              <%= cupon.codigo_cupon %>
            </p>

            <form
              action="/cupones/<%= cupon.id %>/edit"
              method="POST"
              class="space-y-3"
            >
              <input
                type="text"
                name="titulo"
                value="<%= cupon.titulo %>"
                class="w-full p-2 border rounded-lg text-gray-900 focus:ring-4 focus:ring-yellow-300"
                required
              />
              <textarea
                name="descripcion"
                class="w-full p-2 border rounded-lg text-gray-900 focus:ring-4 focus:ring-yellow-300"
                required
              >
<%= cupon.descripcion %></textarea
              >
              <input
                type="text"
                name="codigo_cupon"
                value="<%= cupon.codigo_cupon %>"
                class="w-full p-2 border rounded-lg text-gray-900 focus:ring-4 focus:ring-yellow-300"
                required
              />
              <button
                class="w-full bg-yellow-500 text-white py-2 rounded-lg hover:bg-yellow-600 transition font-semibold"
              >
                Guardar Cambios
              </button>
            </form>

            <form
              id="form-eliminar-cupon-<%= cupon.id %>"
              action="/cupones/<%= cupon.id %>/delete"
              method="POST"
              class="mt-2"
            >
              <button
                type="button"
                onclick="confirmarEliminacionCupon('<%= cupon.id %>')"
                class="w-full bg-red-500 text-white py-2 rounded-lg hover:bg-red-600 transition font-semibold"
              >
                Eliminar Cupón
              </button>
            </form>
          </div>
          <% }) %>
        </div>
      </section>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <!-- <script src="/js/script2.js"></script>
  <script src="/js/script3.js"></script> -->
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script src="/js/alertas.js"></script>
    <script>
      AOS.init({
        duration: 800,
        once: true,
      });
    </script>
  </body>
</html>
