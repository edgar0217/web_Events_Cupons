<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Radio Digital IESPR</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/indexcss.css" />
  </head>
  <body class="min-h-screen text-gray-800 pt-20">
    <!-- Barra de navegación -->
    <%- include("partials/navbar") %>

    <!-- Sección principal -->
    <section
      class="flex flex-col items-center justify-center px-6 md:px-12 lg:px-24 py-16 gap-10"
    >
      <div class="max-w-xl w-full">
        <h1
          class="text-3xl sm:text-4xl font-extrabold text-[#0f172a] mb-4 text-center md:text-left"
        >
          Radio Digital IESPR
        </h1>
        <p class="text-base text-gray-700 mb-6 text-center md:text-left">
          ¡Todo lo que tienes que hacer es disfrutar de nuestra programación!
        </p>
        <!-- Logo + Reproductor alineados -->
        <div
          class="flex flex-col md:flex-row items-center md:items-center gap-6 w-full mb-4"
        >
          <!-- Logo -->
          <div
            class="flex-shrink-0 flex justify-center md:justify-start w-full md:w-auto"
          >
            <img
              src="/img/logo.jpg"
              alt="Logo IESPR"
              class="w-48 h-32 shadow-md object-cover border border-gray-300 rounded-2xl"
            />
          </div>
          <!-- Reproductor -->
          <div class="flex-1 w-full">
            <div
              class="bg-white/90 backdrop-blur-lg rounded-2xl shadow-xl p-6 border border-gray-300 flex flex-col sm:flex-row items-center gap-4 w-full"
            >
              <div class="flex items-center gap-3 w-full sm:w-auto">
                <i class="bx bx-radio text-4xl text-[#0f172a]"></i>
                <div class="text-left">
                  <h2 class="text-lg font-semibold text-gray-800">
                    Radio IESPR
                  </h2>
                  <p class="text-sm text-gray-500">En vivo ahora</p>
                </div>
              </div>
              <!-- Botón de control -->
              <div class="mt-4 sm:mt-0 sm:ml-auto">
                <button
                  id="customPlay"
                  class="bg-black hover:bg-gray-900 text-white p-3 rounded-full shadow transition"
                  aria-label="Play"
                >
                  <i class="bx bx-play text-xl" id="playIcon"></i>
                </button>
              </div>
              <!-- Audio oculto -->
              <audio id="radioPlayer" playsinline>
                <source
                  src="https://stream.zeno.fm/wc0lhuuv1nkvv"
                  type="audio/aac"
                />
                Tu navegador no soporta el elemento de audio.
              </audio>
            </div>
          </div>
        </div>

        <!-- BOTONES EN DESKTOP -->
        <div
          class="hidden sm:flex justify-between items-center gap-4 mt-8 w-full"
        >
          <a
            href="/cupones"
            class="bg-black text-white px-5 py-2 rounded-2xl shadow hover:bg-gray-900 text-sm text-center w-auto"
          >
            Ver Cupones
          </a>
          <button
            id="customPlay2"
            class="bg-black text-white px-5 py-2 rounded-2xl shadow hover:bg-gray-900 text-sm"
          >
            Escuchar Ahora
          </button>
          <a
            href="/eventos"
            class="bg-black text-white px-5 py-2 rounded-2xl shadow hover:bg-gray-900 text-sm text-center w-auto"
          >
            Ver Eventos
          </a>
          <a
            href="https://iespr.edu.mx/revista_iespr.php"
            target="_blank"
            class="bg-black text-white px-5 py-2 rounded-2xl shadow hover:bg-gray-900 text-sm text-center w-auto transition"
          >
            Revista Digital
          </a>
        </div>

        <!-- POLÍTICAS EN DESKTOP -->
        <div class="hidden sm:flex justify-center mt-6 w-full">
          <a
            href="https://radioiespr.cloud/privacy-policy.html"
            target="_blank"
            class="text-sm text-gray-700 hover:underline flex items-center gap-1"
          >
            Políticas de privacidad <span>→</span>
          </a>
        </div>

        <!-- BOTONES Y POLÍTICAS EN MÓVIL (abajo) -->
        <div class="sm:hidden flex flex-col items-center gap-4 w-full">
          <button
            id="customPlay2"
            class="bg-black text-white px-5 py-2 rounded-2xl shadow hover:bg-gray-900 text-sm w-full"
          >
            Escuchar Ahora
          </button>
          <a
            href="/cupones"
            class="bg-black text-white px-5 py-2 rounded-2xl shadow hover:bg-gray-900 text-sm text-center w-full"
          >
            Ver Cupones
          </a>
          <a
            href="/eventos"
            class="bg-black text-white px-5 py-2 rounded-2xl shadow hover:bg-gray-900 text-sm text-center w-full"
          >
            Ver Eventos
          </a>
          <a
            href="https://iespr.edu.mx/revista_iespr.php"
            target="_blank"
            class="bg-black text-white px-5 py-2 rounded-2xl shadow hover:bg-gray-900 text-sm text-center w-full transition"
          >
            Revista Digital
          </a>
          <a
            href="https://radioiespr.cloud/privacy-policy.html"
            target="_blank"
            class="text-sm text-gray-700 hover:underline flex justify-center items-center gap-1 mt-4"
          >
            Políticas de privacidad <span>→</span>
          </a>
        </div>

        <!-- CARRUSEL PATROCINADORES PC (3 x slide) -->
        <div class="w-full flex flex-col items-center mt-10">
          <h2
            class="text-center text-xl sm:text-2xl font-semibold mb-4 text-gray-800"
          >
            Nuestros Patrocinadores
          </h2>
          <div
            class="relative w-full max-w-xs sm:max-w-sm md:max-w-lg lg:max-w-xl mx-auto"
          >
            <!-- Carrusel solo PC -->
            <div
              id="carruselPatrocinadoresPC"
              class="hidden sm:flex relative items-center justify-center overflow-hidden rounded-2xl bg-white/80 shadow-md ring-1 ring-indigo-100 h-60 sm:h-60"
            >
              <% if (listaPatrocinadores.length === 0) { %>
              <p class="text-center text-gray-500 w-full">Próximamente...</p>
              <% } else { %> <% for (let j = 0; j < listaPatrocinadores.length;
              j += 3) { let grupo = listaPatrocinadores.slice(j, j+3); %>
              <div
                class="patrocinador-item-pc absolute left-0 top-0 w-full h-full flex flex-col items-center justify-center transition-opacity duration-700 ease-in-out <% if (j !== 0) { %> opacity-0 pointer-events-none <% } else { %> opacity-100 <% } %>"
                data-index="<%= Math.floor(j/3) %>"
              >
                <div
                  class="w-full h-full flex flex-row gap-4 justify-center items-center"
                >
                  <% grupo.forEach((pat) => { %>
                  <div class="flex flex-col items-center justify-center w-1/3">
                    <img
                      src="<%= pat.imagen_url %>"
                      alt="Patrocinador"
                      class="w-36 h-36 object-cover rounded-2xl mb-2 border-2 border-indigo-100 shadow-lg ring-2 ring-white ring-offset-1 transition-all duration-200"
                      loading="lazy"
                    />
                    <span
                      class="font-bold text-indigo-900 text-lg text-center drop-shadow-sm"
                    >
                      <%= pat.nombre %>
                    </span>
                  </div>
                  <% }) %>
                </div>
              </div>
              <% } %> <% } %>
            </div>
            <!-- Carrusel solo móvil -->
            <div
              id="carruselPatrocinadoresMOVIL"
              class="flex sm:hidden relative items-center justify-center overflow-hidden rounded-2xl bg-white/80 shadow-md ring-1 ring-indigo-100 h-60"
            >
              <% if (listaPatrocinadores.length === 0) { %>
              <p class="text-center text-gray-500 w-full">Próximamente...</p>
              <% } else { %> <% for (let i = 0; i < listaPatrocinadores.length;
              i++) { let pat = listaPatrocinadores[i]; %>
              <div
                class="patrocinador-item-movil absolute left-0 top-0 w-full h-full flex flex-col items-center justify-center transition-opacity duration-700 ease-in-out <% if (i !== 0) { %> opacity-0 pointer-events-none <% } else { %> opacity-100 <% } %>"
                data-index="<%= i %>"
              >
                <div class="flex flex-col items-center justify-center w-full">
                  <img
                    src="<%= pat.imagen_url %>"
                    alt="Patrocinador"
                    class="w-44 h-44 object-cover rounded-2xl mb-2 border-2 border-indigo-100 shadow-lg ring-2 ring-white ring-offset-1 transition-all duration-200"
                    loading="lazy"
                  />
                  <span
                    class="font-bold text-indigo-900 text-base text-center drop-shadow-sm"
                  >
                    <%= pat.nombre %>
                  </span>
                </div>
              </div>
              <% } %> <% } %>
            </div>
            <!-- Flechas PC -->
            <% if (listaPatrocinadores.length > 1) { %>
            <button
              onclick="carruselPrevPC()"
              class="hidden sm:flex absolute left-2 top-1/2 -translate-y-1/2 bg-white hover:bg-indigo-100 text-indigo-600 border border-indigo-200 rounded-full w-9 h-9 items-center justify-center shadow z-10 transition"
              aria-label="Anterior"
              style="backdrop-filter: blur(4px)"
            >
              <i class="bx bx-chevron-left text-2xl"></i>
            </button>
            <button
              onclick="carruselNextPC()"
              class="hidden sm:flex absolute right-2 top-1/2 -translate-y-1/2 bg-white hover:bg-indigo-100 text-indigo-600 border border-indigo-200 rounded-full w-9 h-9 items-center justify-center shadow z-10 transition"
              aria-label="Siguiente"
              style="backdrop-filter: blur(4px)"
            >
              <i class="bx bx-chevron-right text-2xl"></i>
            </button>
            <!-- Flechas MÓVIL -->
            <button
              onclick="carruselPrevMOVIL()"
              class="flex sm:hidden absolute left-2 top-1/2 -translate-y-1/2 bg-white hover:bg-indigo-100 text-indigo-600 border border-indigo-200 rounded-full w-9 h-9 items-center justify-center shadow z-10 transition"
              aria-label="Anterior"
              style="backdrop-filter: blur(4px)"
            >
              <i class="bx bx-chevron-left text-2xl"></i>
            </button>
            <button
              onclick="carruselNextMOVIL()"
              class="flex sm:hidden absolute right-2 top-1/2 -translate-y-1/2 bg-white hover:bg-indigo-100 text-indigo-600 border border-indigo-200 rounded-full w-9 h-9 items-center justify-center shadow z-10 transition"
              aria-label="Siguiente"
              style="backdrop-filter: blur(4px)"
            >
              <i class="bx bx-chevron-right text-2xl"></i>
            </button>
            <% } %>
          </div>
          <% if (listaPatrocinadores.length > 1) { %>
          <!-- Dots PC -->
          <div class="hidden sm:flex justify-center gap-2 mt-3">
            <% const numSlidesPC = Math.ceil(listaPatrocinadores.length / 3);
            for (let d = 0; d < numSlidesPC; d++) { %>
            <button
              class="carrusel-dot-pc w-2.5 h-2.5 rounded-full border border-indigo-200 bg-indigo-100 transition-all duration-300"
              style="<% if (d === 0) { %> background: #4f46e5; border-color: #4f46e5; <% } %>"
              onclick="carruselGoToPC(<%= d %>)"
              aria-label="Ir al grupo <%= d + 1 %> de patrocinadores"
            ></button>
            <% } %>
          </div>
          <!-- Dots MÓVIL -->
          <div class="flex sm:hidden justify-center gap-2 mt-3">
            <% for (let d = 0; d < listaPatrocinadores.length; d++) { %>
            <button
              class="carrusel-dot-movil w-2.5 h-2.5 rounded-full border border-indigo-200 bg-indigo-100 transition-all duration-300"
              style="<% if (d === 0) { %> background: #4f46e5; border-color: #4f46e5; <% } %>"
              onclick="carruselGoToMOVIL(<%= d %>)"
              aria-label="Ir al patrocinador <%= d + 1 %>"
            ></button>
            <% } %>
          </div>
          <% } %>
        </div>
        <!-- FIN DEL CARRUSEL -->
      </div>
    </section>

    <script src="/js/indexjs.js"></script>
  </body>
</html>
